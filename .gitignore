# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon?
._*

# Editors / IDEs
.vscode/
.idea/
*.swp
*~

# Node (only if the site later grows a toolchain)
node_modules/
dist/
.cache/

# Xcode / Swift
build/
.build/
DerivedData/
*.xcuserstate
xcuserdata/
*.xcworkspace/xcuserdata/
.swiftpm/
# Package.resolved is committed on purpose — dependency pins are part of the plan (KTD5)

# Secrets — never commit the waitlist endpoint or keys
.env
.env.local

# Vercel
.vercel
